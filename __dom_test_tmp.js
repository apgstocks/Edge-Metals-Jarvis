// Leftover scratch file from a jsdom test run during development — safe to
// delete. The sandbox this was created in wouldn't allow rm on it, so it's
// emptied instead so it doesn't carry stale test code if it's ever opened.
// Not referenced by anything in the app.
